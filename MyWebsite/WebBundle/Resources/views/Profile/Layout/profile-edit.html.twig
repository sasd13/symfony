<div class="feature-grid2">
	<div class="feature-grid-head feature-grid-head3">
		<h3><span>Modifier</span> Profil</h3>
		<div class="feature-grid-head-top">
			<label> </label><label> </label>
			<div class="clear"> </div>
		</div>
	</div>
	<div class="new-form">
		<form class="contact_form" action="{{ path('web_profile_edit') }}" method="post" name="contact_form">
			<div class="form-submit2">
				<input type="submit" class="submit" value="Enregistrer"/>
				{% if message is defined %}
					<div class="required_notification">{{ message }}</div>
				{% else %}
					<span class="required_notification">* Denotes Required Field</span>
				{% endif %}
			</div>
			<br/>
			<ul class="form-content">
				<br/>
				<li>
					<h2>Informations personnelles</h2>
				</li>
				<li>
					{{ form_label(formProfile.firstName, "Prénom :") }}
					{{ form_widget(formProfile.firstName) }}
				</li>
				<li>
					{{ form_label(formProfile.lastName, "Nom :") }}
					{{ form_widget(formProfile.lastName) }}
				</li>
				{% if categories is defined %}
					{% for formCategory in formsViewsCategories %}
						{% set category = attribute(categories, loop.index0) %}
						{% include "MyWebsiteWebBundle:Profil:Layout:Form:profile-category-form.html.twig" %}
					{% endfor %}
				{% endif %}
			</ul>
		</form>
	</div>
	<div class="clear"> </div>
</div>